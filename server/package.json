{
  "name": "aurex-server",
  "version": "1.0.0",
  "description": "Backend do Aurex in Chrome: autenticação OAuth (PKCE + Google) e endpoint /v1/chat/completions.",
  "type": "module",
  "main": "src/index.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.22.2",
    "jsonwebtoken": "^9.0.2",
    "pg": "^8.12.0"
  },
  "//overrides": "body-parser não é dependência nossa — o express a traz. O override eleva o piso da versão transitiva (CVE-2026-12590, DoS via opção limit inválida) sem fingir que a importamos.",
  "overrides": {
    "body-parser": "^1.20.6"
  }
}
