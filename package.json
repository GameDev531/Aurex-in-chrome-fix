{
  "name": "aurex-in-chrome",
  "version": "1.1.0",
  "private": true,
  "description": "Aurex in Chrome — extensao de navegador com agente. Testes da extensao.",
  "scripts": {
    "test": "node tests/run.js",
    "check": "for f in *.js; do case \"$f\" in *.min.js) continue;; esac; node --check \"$f\" || exit 1; done"
  }
}
